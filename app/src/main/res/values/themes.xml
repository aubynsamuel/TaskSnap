<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.TaskSnap" parent="android:Theme.Material.Light.NoActionBar" />

    <!-- Transparent theme for dialog-like activities (ProcessTextActivity) -->
    <style name="Theme.TaskSnap.Transparent" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:backgroundDimEnabled">true</item>
    </style>

    <!-- Transparent theme for dialog-like activities (ProcessTextActivity) -->
    <style name="Theme.TaskSnap.OverlayDialog" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:windowCloseOnTouchOutside">true</item>
    </style>

    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenAnimationDuration">1400</item>
        <item name="windowSplashScreenBackground">@color/white</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/task_snap_launcher_animator</item>
        <item name="postSplashScreenTheme">@style/Theme.TaskSnap</item>
    </style>

</resources>